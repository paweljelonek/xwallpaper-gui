# No pip packages required.
# All dependencies are system packages — install with your package manager:
#
#   sudo apt install xwallpaper python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-gdkpixbuf-2.0
#
# In a virtualenv, expose system gi with:
#   echo "/usr/lib/python3/dist-packages" > \
#     "$(python -c 'import site; print(site.getsitepackages()[0])')/system-gi.pth"
